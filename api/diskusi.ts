import { db } from '@/utils/firebase'
import { Diskusi } from '../model/diskusi'

function createDiskusiRef(shopid: string){

}

export async function getDiskusi (shopid: string): Promise<Diskusi[]> {
  return []
}
